[flake8]
max-line-length = 99
exclude = **/migrations/*,venv, Notes/, maintenance_scripts/,

[tool:pytest]
DJANGO_SETTINGS_MODULE = config.settings
minversion = 6.0
addopts = -ra -q
testpaths =
    site_tests
    integration